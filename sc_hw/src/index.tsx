import React from 'react';
import ReactDOM from 'react-dom/client';
import Sitavikat from './App';

const root = ReactDOM.createRoot(
  document.getElementById('root') as HTMLElement
);

root.render(
  <React.StrictMode>
    <Sitavikat />
  </React.StrictMode>
);